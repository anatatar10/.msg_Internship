import { AirportDisplayPipe } from './airport-display.pipe';

describe('AirportDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new AirportDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
